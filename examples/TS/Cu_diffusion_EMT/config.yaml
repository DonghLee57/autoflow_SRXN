engine:
  potential:
    backend: emt
    device: cpu
paths:
  initial_state: "inputs/initial.vasp"
  final_state: "inputs/final.vasp"
  output_dir: results
relaxation:
  fmax: 0.05
  steps: 200
reaction_search:
  mechanisms:
    precursor:
      ts_search:
        enabled: true
        n_images: 5
        fmax: 0.05
        steps: 100
